const mongoose = require('mongoose');

mongoose.connect('mongodb://localhost/listings/:listing_id/recommendations');

// let seedDB = function(data) {
//   for (var i = 0; i < data.length; i++) {

//   }
// };
